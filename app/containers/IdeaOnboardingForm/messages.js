/*
 * IdeaOnboardingForm Messages
 *
 * This contains all the text for the IdeaOnboardingForm container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.IdeaOnboardingForm";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the IdeaOnboardingForm container!"
  }
});
