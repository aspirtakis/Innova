/*
 * OnboardingForm Messages
 *
 * This contains all the text for the OnboardingForm container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.OnboardingForm";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the OnboardingForm container!"
  }
});
