// import { selectOnboardingFormDomain } from '../selectors';

describe("selectOnboardingFormDomain", () => {
  it("Expect to have unit tests specified", () => {
    expect(true).toEqual(false);
  });
});
