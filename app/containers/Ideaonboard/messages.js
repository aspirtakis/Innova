/*
 * Ideaonboard Messages
 *
 * This contains all the text for the Ideaonboard container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.Ideaonboard";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the Ideaonboard container!"
  }
});
