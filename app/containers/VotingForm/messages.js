/*
 * VotingForm Messages
 *
 * This contains all the text for the VotingForm container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.VotingForm";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the VotingForm container!"
  }
});
