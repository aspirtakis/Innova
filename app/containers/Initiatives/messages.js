/*
 * Initiatives Messages
 *
 * This contains all the text for the Initiatives container.
 */

import { defineMessages } from "react-intl";

export const scope = "app.containers.Initiatives";

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: "This is the Initiatives container!"
  }
});
